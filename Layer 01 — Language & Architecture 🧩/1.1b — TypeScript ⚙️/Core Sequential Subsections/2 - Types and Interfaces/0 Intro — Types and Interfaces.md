# Types and Interfaces — Summary

This directory focuses on modeling domain shapes and reusable APIs.

You’ll learn when to use `type` vs `interface`, how generics keep utilities type-safe,
and how utility types prevent duplication.

## Outcomes

- Choose `type` vs `interface` intentionally.
- Write basic generics with constraints.
- Use utility types without losing readability.
